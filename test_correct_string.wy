注曰「Test Correct String Syntax」
注曰「Author: Whisky, PR Worker」

注曰「In wenyan, string literals need special syntax」
吾有一言。名之曰「測試字符串」。
吾有一言。曰「測試值」。名之曰「測試字符串」。

注曰「Test function parameter string comparison」
吾有一術。名之曰「測試函數」。欲行是術。必先得一物。曰「輸入類型」。乃行是術曰。
    若「輸入類型」等於「加法」者。
        乃得「加法結果」。
    云云。
    若「輸入類型」等於「減法」者。
        乃得「減法結果」。
    云云。
    乃得「未知操作」。
是謂「測試函數」之術也。

注曰「Test function calls」
吾有一言。曰「加法」。名之曰「操作一」。
吾有一言。曰「減法」。名之曰「操作二」。
吾有一言。曰「乘法」。名之曰「操作三」。

吾有一言。名之曰「結果一」。
施「測試函數」於「操作一」。名之曰「結果一」。

吾有一言。名之曰「結果二」。
施「測試函數」於「操作二」。名之曰「結果二」。

吾有一言。名之曰「結果三」。
施「測試函數」於「操作三」。名之曰「結果三」。

書之「String function test completed」。