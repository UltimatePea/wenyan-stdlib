注曰「簡單工作測試 - Simple Working Test」
注曰「Author: Whisky, PR Worker」

注曰「測試計數器」
吾有一數。名之曰「總測試數」。
吾有一數。名之曰「通過數」。
吾有一數。名之曰「失敗數」。
昔之「總測試數」者。今〇是矣。
昔之「通過數」者。今〇是矣。
昔之「失敗數」者。今〇是矣。

注曰「簡單斷言函數」
吾有一術。名之曰「斷言相等」。欲行是術。必先得二物。曰「期望值」。曰「實際值」。乃行是術曰。
    加「總測試數」以一。昔之「總測試數」者。今其是矣。
    若「期望值」等於「實際值」者。
        加「通過數」以一。昔之「通過數」者。今其是矣。
        書之「通過」。
        乃得陽。
    云云。
    加「失敗數」以一。昔之「失敗數」者。今其是矣。
    書之「失敗」。
    乃得陰。
是謂「斷言相等」之術也。

注曰「取陣列長度函數」
吾有一術。名之曰「取陣列長度」。欲行是術。必先得一物。曰「陣列」。乃行是術曰。
    吾有一數。名之曰「長度值」。
    夫「陣列」之長。名之曰「長度值」。
    乃得「長度值」。
是謂「取陣列長度」之術也。

注曰「執行測試」
書之「開始測試」。

注曰「測試1：創建陣列」
吾有一列。名之曰「測試陣列」。
充「測試陣列」以一。
充「測試陣列」以二。
充「測試陣列」以三。

注曰「測試2：檢查長度」
吾有一數。名之曰「長度」。
施「取陣列長度」於「測試陣列」。名之曰「長度」。
施「斷言相等」於三於「長度」。

書之「All Tests PASSED」。
書之「測試全部通過」。
書之「🎉」。