注曰「Standalone Comprehensive Array Test」
注曰「Author: Whisky, PR Worker」
注曰「Tests all three critical generic functions inline」

注曰「陣列創建函數」
吾有一術。名之曰「陣列創建」。欲行是術。必先得二物。曰「大小」。曰「初始值」。乃行是術曰。
    若「大小」小於〇者。
        吾有一列。名之曰「空陣列」。
        乃得「空陣列」。
    云云。
    
    吾有一列。名之曰「新陣列」。
    吾有一數。名之曰「索引」。
    昔之「索引」者。今一是矣。
    
    恆為是。
        若「索引」大於「大小」者。乃止。云云。
        充「新陣列」以「初始值」。
        加「索引」以一。昔之「索引」者。今其是矣。
    云云。
    
    乃得「新陣列」。
是謂「陣列創建」之術也。

注曰「取陣列長度函數」
吾有一術。名之曰「取陣列長度」。欲行是術。必先得一物。曰「陣列」。乃行是術曰。
    乃得「陣列」之長。
是謂「取陣列長度」之術也。

注曰「取陣列元素函數」  
吾有一術。名之曰「取陣列元素」。欲行是術。必先得二物。曰「陣列」。曰「索引」。乃行是術曰。
    若「索引」小於一者。
        乃得〇。
    云云。
    若「索引」大於「陣列」之長者。
        乃得〇。
    云云。
    
    夫「陣列」之「索引」。乃得其。
是謂「取陣列元素」之術也。

注曰「平方變換函數」
吾有一術。名之曰「平方變換」。欲行是術。必先得一物。曰「數值」。乃行是術曰。
    乘「數值」以「數值」。乃得其。
是謂「平方變換」之術也。

注曰「加法聚合函數」
吾有一術。名之曰「加法聚合」。欲行是術。必先得二物。曰「累積值」。曰「當前元素」。乃行是術曰。
    加「累積值」以「當前元素」。乃得其。
是謂「加法聚合」之術也。

注曰「陣列映射函數 - 通用版本」
吾有一術。名之曰「陣列映射」。欲行是術。必先得二物。曰「陣列」。曰「變換函數」。乃行是術曰。
    吾有一列。名之曰「映射陣列」。
    吾有一數。名之曰「索引」。
    昔之「索引」者。今一是矣。
    
    恆為是。
        若「索引」大於「陣列」之長者。乃止。云云。
        夫「陣列」之「索引」。名之曰「當前元素」。
        施「變換函數」於「當前元素」。名之曰「結果元素」。
        充「映射陣列」以「結果元素」。
        加「索引」以一。昔之「索引」者。今其是矣。
    云云。
    
    乃得「映射陣列」。
是謂「陣列映射」之術也。

注曰「陣列折疊函數 - 通用版本」
吾有一術。名之曰「陣列折疊」。欲行是術。必先得三物。曰「陣列」。曰「初始值」。曰「聚合函數」。乃行是術曰。
    吾有一物。名之曰「累積值」。
    昔之「累積值」者。今「初始值」是矣。
    吾有一數。名之曰「索引」。
    昔之「索引」者。今一是矣。
    
    恆為是。
        若「索引」大於「陣列」之長者。乃止。云云。
        夫「陣列」之「索引」。名之曰「當前元素」。
        夫「累積值」。夫「當前元素」。取二以施「聚合函數」。名之曰「累積值」。
        加「索引」以一。昔之「索引」者。今其是矣。
    云云。
    
    乃得「累積值」。
是謂「陣列折疊」之術也。

注曰「測試開始」
夫五。夫十。取二以施「陣列創建」。名之曰「測試陣列」。

注曰「測試陣列長度」
施「取陣列長度」於「測試陣列」。書之。

注曰「測試陣列映射」
夫「測試陣列」。夫「平方變換」。取二以施「陣列映射」。名之曰「平方結果」。
夫「平方結果」。夫一。取二以施「取陣列元素」。書之。

注曰「測試陣列折疊」
夫「測試陣列」。夫〇。夫「加法聚合」。取三以施「陣列折疊」。書之。