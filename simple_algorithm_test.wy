/* 簡單算法測試 - Author: Whisky, PR Worker */

/* 載入算經庫 */
吾嘗試載入「「libs/算經/算經」」。

/* 測試數字轉字符串算法 */
吾有一數。名之曰「測試數」。昔之「測試數」者。今一二三四五也。
施「數字轉字符串」於「測試數」。名之曰「字符串結果」。
書「測試數字轉字符串：」。書「字符串結果」。

/* 測試格式化小數算法 */
吾有一數。名之曰「小數測試」。昔之「小數測試」者。今3.14159也。
施「格式化小數」於「小數測試」於二。名之曰「小數結果」。
書「測試格式化小數：」。書「小數結果」。