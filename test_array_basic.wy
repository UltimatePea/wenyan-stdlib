注曰「Basic Array Function Tests」
注曰「Author: Whisky, PR Worker」

注曰「Test 1: Array Creation」
吾有一列。名之曰「測試陣列」。
充「測試陣列」以一。
充「測試陣列」以二。
充「測試陣列」以三。

書之「創建的陣列：」。書之「測試陣列」。

注曰「Test 2: Array Length」
吾有一數。名之曰「長度」。
夫「測試陣列」之長。名之曰「長度」。
書之「陣列長度：」。書之「長度」。

注曰「Test 3: Array Element Access」
吾有一物。名之曰「元素一」。
夫「測試陣列」之一。名之曰「元素一」。
書之「第一個元素：」。書之「元素一」。

吾有一物。名之曰「元素二」。
夫「測試陣列」之二。名之曰「元素二」。
書之「第二個元素：」。書之「元素二」。

注曰「Test 4: Array Sum」
吾有一數。名之曰「總和」。
昔之「總和」者。今〇是矣。
吾有一數。名之曰「索引」。
昔之「索引」者。今一是矣。

恆為是。
    若「索引」大於「長度」者。乃止。云云。
    吾有一數。名之曰「當前元素」。
    夫「測試陣列」之「索引」。名之曰「當前元素」。
    加「總和」以「當前元素」。昔之「總和」者。今其是矣。
    加「索引」以一。昔之「索引」者。今其是矣。
云云。

書之「陣列總和：」。書之「總和」。

注曰「Test 5: Array Concatenation」
吾有一列。名之曰「陣列甲」。
充「陣列甲」以十。
充「陣列甲」以二十。

吾有一列。名之曰「拼接結果」。
吾有一數。名之曰「索引甲」。
吾有一數。名之曰「索引乙」。
吾有一數。名之曰「甲長度」。
吾有一數。名之曰「乙長度」。
吾有一物。名之曰「當前元素」。

夫「測試陣列」之長。名之曰「甲長度」。
夫「陣列甲」之長。名之曰「乙長度」。

昔之「索引甲」者。今一是矣。
恆為是。
    若「索引甲」大於「甲長度」者。乃止。云云。
    夫「測試陣列」之「索引甲」。名之曰「當前元素」。
    充「拼接結果」以「當前元素」。
    加「索引甲」以一。昔之「索引甲」者。今其是矣。
云云。

昔之「索引乙」者。今一是矣。
恆為是。
    若「索引乙」大於「乙長度」者。乃止。云云。
    夫「陣列甲」之「索引乙」。名之曰「當前元素」。
    充「拼接結果」以「當前元素」。
    加「索引乙」以一。昔之「索引乙」者。今其是矣。
云云。

書之「拼接結果：」。書之「拼接結果」。

注曰「Test Complete」