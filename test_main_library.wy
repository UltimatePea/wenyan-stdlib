注曰「Test Main Library Functions」
注曰「Author: Whisky, PR Worker」

注曰「Import the main library by copying the functions we need」

注曰「取陣列長度函數」
吾有一術。名之曰「取陣列長度」。欲行是術。必先得一物。曰「陣列」。乃行是術曰。
    吾有一數。名之曰「長度」。
    夫「陣列」之長。名之曰「長度」。
    乃得「長度」。
是謂「取陣列長度」之術也。

注曰「取陣列指定位置的元素（1-based索引）」
吾有一術。名之曰「取陣列元素」。欲行是術。必先得二物。曰「陣列」。曰「索引」。乃行是術曰。
    若「索引」小於一者。
        乃得〇。
    云云。
    若「索引」大於「陣列」之長者。
        乃得〇。
    云云。
    
    吾有一物。名之曰「元素」。
    夫「陣列」之「索引」。名之曰「元素」。
    乃得「元素」。
是謂「取陣列元素」之術也。

注曰「計算數字陣列的總和」
吾有一術。名之曰「陣列求和」。欲行是術。必先得一物。曰「陣列」。乃行是術曰。
    吾有一數。名之曰「總和」。
    吾有一數。名之曰「索引」。
    吾有一數。名之曰「長度」。
    吾有一數。名之曰「當前元素」。
    
    夫「陣列」之長。名之曰「長度」。
    昔之「總和」者。今〇是矣。
    昔之「索引」者。今一是矣。
    
    恆為是。
        若「索引」大於「長度」者。乃止。云云。
        夫「陣列」之「索引」。名之曰「當前元素」。
        加「總和」以「當前元素」。昔之「總和」者。今其是矣。
        加「索引」以一。昔之「索引」者。今其是矣。
    云云。
    
    乃得「總和」。
是謂「陣列求和」之術也。

注曰「Now test the functions」
吾有一列。名之曰「測試陣列」。
充「測試陣列」以五。
充「測試陣列」以十。
充「測試陣列」以十五。

注曰「Test 1: Array length」
吾有一數。名之曰「長度結果」。
施「取陣列長度」於「測試陣列」。名之曰「長度結果」。

注曰「Test 2: Element access」
吾有一數。名之曰「元素結果」。
施「取陣列元素」於「測試陣列」於二。名之曰「元素結果」。

注曰「Test 3: Array sum」
吾有一數。名之曰「求和結果」。
施「陣列求和」於「測試陣列」。名之曰「求和結果」。

書之「All library tests completed」。