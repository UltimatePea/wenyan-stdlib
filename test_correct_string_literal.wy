注曰「Test Correct String Literal Syntax」
注曰「Author: Whisky, PR Worker」

注曰「Correct string literal definition」
吾有一言。曰「「測試值」」。名之曰「測試字符串」。

注曰「Test function with string comparison」
吾有一術。名之曰「操作函數」。欲行是術。必先得一物。曰「操作類型」。乃行是術曰。
    吾有一言。曰「「加法」」。名之曰「加法字面量」。
    吾有一言。曰「「減法」」。名之曰「減法字面量」。
    吾有一言。曰「「乘法」」。名之曰「乘法字面量」。
    
    若「操作類型」等於「加法字面量」者。
        吾有一言。曰「「加法結果」」。名之曰「結果」。
        乃得「結果」。
    云云。
    若「操作類型」等於「減法字面量」者。
        吾有一言。曰「「減法結果」」。名之曰「結果」。
        乃得「結果」。
    云云。
    若「操作類型」等於「乘法字面量」者。
        吾有一言。曰「「乘法結果」」。名之曰「結果」。
        乃得「結果」。
    云云。
    
    吾有一言。曰「「未知操作」」。名之曰「默認結果」。
    乃得「默認結果」。
是謂「操作函數」之術也。

注曰「Test function calls」
吾有一言。曰「「加法」」。名之曰「操作一」。
吾有一言。曰「「減法」」。名之曰「操作二」。
吾有一言。曰「「乘法」」。名之曰「操作三」。

吾有一言。名之曰「結果一」。
施「操作函數」於「操作一」。名之曰「結果一」。

吾有一言。名之曰「結果二」。
施「操作函數」於「操作二」。名之曰「結果二」。

吾有一言。名之曰「結果三」。
施「操作函數」於「操作三」。名之曰「結果三」。

書之「String literal test completed」。