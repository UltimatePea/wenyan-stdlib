注曰「Test Library Function」
注曰「Author: Whisky, PR Worker」

注曰「Define a simple version of array length function」
吾有一術。名之曰「取陣列長度」。欲行是術。必先得一物。曰「陣列」。乃行是術曰。
    吾有一數。名之曰「長度」。
    夫「陣列」之長。名之曰「長度」。
    乃得「長度」。
是謂「取陣列長度」之術也。

注曰「Test the function」
吾有一列。名之曰「測試陣列」。
充「測試陣列」以一。
充「測試陣列」以二。
充「測試陣列」以三。

吾有一數。名之曰「結果」。
施「取陣列長度」於「測試陣列」。名之曰「結果」。

書之「Result completed」。