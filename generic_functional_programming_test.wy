注曰「Generic Functional Programming Test」
注曰「Author: Whisky, PR Worker - Testing Delta's critical requirements」
注曰「Test generic map, fold, and sort functions with custom functions」

吾嘗觀「/home/zc/worktrees/wenyan-stdlib/libs/列經/列經.wy」之書。

注曰「測試一：Generic Map Function with Custom Transformation Functions」
書之「=== 測試通用映射函數 ==="。

注曰「創建測試陣列」
吾有一列。名之曰「測試陣列」。
充「測試陣列」以一。
充「測試陣列」以二。
充「測試陣列」以三。
充「測試陣列」以四。
充「測試陣列」以五。

書之「原始陣列：」。
吾有一數。名之曰「索引」。
吾有一數。名之曰「長度」。
夫「測試陣列」之長。名之曰「長度」。
昔之「索引」者。今一是矣。

恆為是。
    若「索引」大於「長度」者。乃止。云云。
    吾有一物。名之曰「元素」。
    夫「測試陣列」之「索引」。名之曰「元素」。
    書之「元素」。書之「」。
    加「索引」以一。昔之「索引」者。今其是矣。
云云。
書之「」。

注曰「測試1.1：使用平方變換函數」
書之「測試1.1：平方映射」。
吾有一列。名之曰「平方結果」。
夫「測試陣列」。夫「平方變換」。取二以施「陣列映射」。名之曰「平方結果」。

書之「平方結果：」。
夫「平方結果」之長。名之曰「長度」。
昔之「索引」者。今一是矣。

恆為是。
    若「索引」大於「長度」者。乃止。云云。
    夫「平方結果」之「索引」。名之曰「元素」。
    書之「元素」。書之「」。
    加「索引」以一。昔之「索引」者。今其是矣。
云云。
書之「」。

注曰「測試1.2：使用絕對值變換函數」
書之「測試1.2：絕對值映射（負數陣列）」。
吾有一列。名之曰「負數陣列」。
充「負數陣列」以負三。
充「負數陣列」以負二。
充「負數陣列」以負一。
充「負數陣列」以一。
充「負數陣列」以二。

吾有一列。名之曰「絕對值結果」。
夫「負數陣列」。夫「絕對值變換」。取二以施「陣列映射」。名之曰「絕對值結果」。

書之「絕對值結果：」。
夫「絕對值結果」之長。名之曰「長度」。
昔之「索引」者。今一是矣。

恆為是。
    若「索引」大於「長度」者。乃止。云云。
    夫「絕對值結果」之「索引」。名之曰「元素」。
    書之「元素」。書之「」。
    加「索引」以一。昔之「索引」者。今其是矣。
云云。
書之「」。

注曰「測試二：Generic Fold Function with Custom Aggregation Functions」
書之「=== 測試通用折疊函數 ==="。

注曰「測試2.1：使用加法聚合函數求和」
書之「測試2.1：加法聚合求和」。
吾有一數。名之曰「加法結果」。
夫「測試陣列」。夫〇。夫「加法聚合」。取三以施「陣列折疊」。名之曰「加法結果」。
書之「加法聚合結果：」。書之「加法結果」。

注曰「測試2.2：使用乘法聚合函數求積」
書之「測試2.2：乘法聚合求積」。
吾有一數。名之曰「乘法結果」。
夫「測試陣列」。夫一。夫「乘法聚合」。取三以施「陣列折疊」。名之曰「乘法結果」。
書之「乘法聚合結果：」。書之「乘法結果」。

注曰「測試2.3：使用最大值聚合函數」
書之「測試2.3：最大值聚合」。
吾有一數。名之曰「最大值結果」。
夫「測試陣列」。夫〇。夫「最大值聚合」。取三以施「陣列折疊」。名之曰「最大值結果」。
書之「最大值聚合結果：」。書之「最大值結果」。

注曰「測試2.4：使用最小值聚合函數」
書之「測試2.4：最小值聚合」。
吾有一數。名之曰「最小值結果」。
夫「測試陣列」。夫十。夫「最小值聚合」。取三以施「陣列折疊」。名之曰「最小值結果」。
書之「最小值聚合結果：」。書之「最小值結果」。

注曰「測試三：Generic Sort Function with Custom Comparison Functions」
書之「=== 測試通用排序函數 ==="。

注曰「創建亂序測試陣列」
吾有一列。名之曰「亂序陣列」。
充「亂序陣列」以五。
充「亂序陣列」以二。
充「亂序陣列」以八。
充「亂序陣列」以一。
充「亂序陣列」以九。
充「亂序陣列」以三。

書之「原始亂序陣列：」。
夫「亂序陣列」之長。名之曰「長度」。
昔之「索引」者。今一是矣。

恆為是。
    若「索引」大於「長度」者。乃止。云云。
    夫「亂序陣列」之「索引」。名之曰「元素」。
    書之「元素」。書之「」。
    加「索引」以一。昔之「索引」者。今其是矣。
云云。
書之「」。

注曰「測試3.1：使用升序比較函數」
書之「測試3.1：升序排序」。
吾有一列。名之曰「升序結果」。
夫「亂序陣列」。夫「升序比較」。取二以施「陣列排序」。名之曰「升序結果」。

書之「升序排序結果：」。
夫「升序結果」之長。名之曰「長度」。
昔之「索引」者。今一是矣。

恆為是。
    若「索引」大於「長度」者。乃止。云云。
    夫「升序結果」之「索引」。名之曰「元素」。
    書之「元素」。書之「」。
    加「索引」以一。昔之「索引」者。今其是矣。
云云。
書之「」。

注曰「測試3.2：使用降序比較函數」
書之「測試3.2：降序排序」。
吾有一列。名之曰「降序結果」。
夫「亂序陣列」。夫「降序比較」。取二以施「陣列排序」。名之曰「降序結果」。

書之「降序排序結果：」。
夫「降序結果」之長。名之曰「長度」。
昔之「索引」者。今一是矣。

恆為是。
    若「索引」大於「長度」者。乃止。云云。
    夫「降序結果」之「索引」。名之曰「元素」。
    書之「元素」。書之「」。
    加「索引」以一。昔之「索引」者。今其是矣。
云云。
書之「」。

注曰「測試3.3：使用絕對值比較函數」
書之「測試3.3：絕對值排序」。
吾有一列。名之曰「混合陣列」。
充「混合陣列」以負五。
充「混合陣列」以三。
充「混合陣列」以負二。
充「混合陣列」以七。
充「混合陣列」以負一。

書之「混合數值陣列（絕對值排序前）：」。
夫「混合陣列」之長。名之曰「長度」。
昔之「索引」者。今一是矣。

恆為是。
    若「索引」大於「長度」者。乃止。云云。
    夫「混合陣列」之「索引」。名之曰「元素」。
    書之「元素」。書之「」。
    加「索引」以一。昔之「索引」者。今其是矣。
云云。
書之「」。

吾有一列。名之曰「絕對值排序結果」。
夫「混合陣列」。夫「絕對值比較」。取二以施「陣列排序」。名之曰「絕對值排序結果」。

書之「絕對值排序結果：」。
夫「絕對值排序結果」之長。名之曰「長度」。
昔之「索引」者。今一是矣。

恆為是。
    若「索引」大於「長度」者。乃止。云云。
    夫「絕對值排序結果」之「索引」。名之曰「元素」。
    書之「元素」。書之「」。
    加「索引」以一。昔之「索引」者。今其是矣。
云云。
書之「」。

書之「=== 通用函數編程模式測試完成 ==="。
書之「所有通用函數（映射、折疊、排序）均支持自定義函數參數」。