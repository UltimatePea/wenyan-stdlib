注曰「Test Fixed Library Functions」
注曰「Author: Whisky, PR Worker」

注曰「Import key functions from the fixed library」

注曰「陣列映射預定義函數」
吾有一術。名之曰「陣列映射預定義」。欲行是術。必先得三物。曰「陣列」。曰「操作類型」。曰「參數」。乃行是術曰。
    吾有一列。名之曰「映射陣列」。
    吾有一數。名之曰「索引」。
    吾有一數。名之曰「長度」。
    吾有一物。名之曰「當前元素」。
    吾有一物。名之曰「結果元素」。
    
    注曰「定義操作類型字符串常量」
    吾有一言。曰「「乘法」」。名之曰「乘法類型」。
    吾有一言。曰「「加法」」。名之曰「加法類型」。
    
    夫「陣列」之長。名之曰「長度」。
    昔之「索引」者。今一是矣。
    
    恆為是。
        若「索引」大於「長度」者。乃止。云云。
        夫「陣列」之「索引」。名之曰「當前元素」。
        
        若「操作類型」等於「乘法類型」者。
            乘「當前元素」以「參數」。名之曰「結果元素」。
        云云。
        若「操作類型」等於「加法類型」者。
            加「當前元素」以「參數」。名之曰「結果元素」。
        云云。
        
        充「映射陣列」以「結果元素」。
        加「索引」以一。昔之「索引」者。今其是矣。
    云云。
    
    乃得「映射陣列」。
是謂「陣列映射預定義」之術也。

注曰「Test the mapping function」
吾有一列。名之曰「測試陣列」。
充「測試陣列」以二。
充「測試陣列」以三。
充「測試陣列」以四。

吾有一言。曰「「乘法」」。名之曰「乘法操作」。
吾有一數。曰二。名之曰「參數」。

吾有一列。名之曰「映射結果」。
施「陣列映射預定義」於「測試陣列」於「乘法操作」於「參數」。名之曰「映射結果」。

書之「Fixed mapping test completed」。