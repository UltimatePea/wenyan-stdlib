注曰「測試核心陣列函數」
注曰「Author: Whisky, PR Worker」

注曰「直接包含陣列函數定義」

注曰「創建測試陣列」
吾有一列。名之曰「測試陣列」。
充「測試陣列」以一。以二。以三。

注曰「測試取陣列長度」
書之「=== 測試取陣列長度 ===」。
吾有一數。名之曰「長度結果」。
施「取陣列長度」於「測試陣列」。名之曰「長度結果」。
書之「陣列長度：」。
書之「長度結果」。

注曰「測試陣列為空」
書之「=== 測試陣列為空 ===」。
吾有一爻。名之曰「空結果」。
施「陣列為空」於「測試陣列」。名之曰「空結果」。
書之「陣列是否為空：」。
書之「空結果」。

注曰「測試取陣列元素」
書之「=== 測試取陣列元素 ===」。
吾有一數。名之曰「第一元素」。
施「取陣列元素」於「測試陣列」、一。名之曰「第一元素」。
書之「第一個元素：」。
書之「第一元素」。

吾有一數。名之曰「第二元素」。
施「取陣列元素」於「測試陣列」、二。名之曰「第二元素」。
書之「第二個元素：」。
書之「第二元素」。

注曰「測試陣列追加」
書之「=== 測試陣列追加 ===」。
吾有一列。名之曰「新陣列」。
施「陣列追加」於「測試陣列」、四。名之曰「新陣列」。
吾有一數。名之曰「新長度」。
施「取陣列長度」於「新陣列」。名之曰「新長度」。
書之「追加元素後的長度：」。
書之「新長度」。