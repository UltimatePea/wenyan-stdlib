注曰「簡單陣列函數測試」
注曰「Author: Whisky, PR Worker」

注曰「取陣列長度函數」
吾有一術。名之曰「取陣列長度」。欲行是術。必先得一物。曰「陣列」。乃行是術曰。
    吾有一數。名之曰「長度」。
    夫「陣列」之長。名之曰「長度」。
    乃得「長度」。
是謂「取陣列長度」之術也。

注曰「取陣列元素函數」
吾有一術。名之曰「取陣列元素」。欲行是術。必先得二物。曰「陣列」。曰「索引」。乃行是術曰。
    吾有一物。名之曰「元素」。
    夫「陣列」之「索引」。名之曰「元素」。
    乃得「元素」。
是謂「取陣列元素」之術也。

注曰「創建測試陣列」
吾有一列。名之曰「測試陣列」。
充「測試陣列」以一。以二。以三。

注曰「測試長度」
書之「測試陣列長度：」。
吾有一數。名之曰「長度結果」。
施「取陣列長度」於「測試陣列」。名之曰「長度結果」。
書之「長度結果」。

注曰「測試元素存取」
書之「測試第一個元素：」。
吾有一數。名之曰「第一元素」。
夫「測試陣列」。夫一。取二以施「取陣列元素」。名之曰「第一元素」。
書之「第一元素」。

書之「測試第二個元素：」。
吾有一數。名之曰「第二元素」。
夫「測試陣列」。夫二。取二以施「取陣列元素」。名之曰「第二元素」。
書之「第二元素」。