注曰「列經核心函數綜合測試」
注曰「Author: Whisky, PR Worker」

注曰「引入核心函數定義」
吾有一術。名之曰「取陣列長度」。欲行是術。必先得一物。曰「陣列」。乃行是術曰。
    吾有一數。名之曰「長度」。
    夫「陣列」之長。名之曰「長度」。
    乃得「長度」。
是謂「取陣列長度」之術也。

吾有一術。名之曰「取陣列元素」。欲行是術。必先得二物。曰「陣列」。曰「索引」。乃行是術曰。
    吾有一物。名之曰「元素」。
    夫「陣列」之「索引」。名之曰「元素」。
    乃得「元素」。
是謂「取陣列元素」之術也。

吾有一術。名之曰「陣列追加」。欲行是術。必先得二物。曰「陣列」。曰「新元素」。乃行是術曰。
    充「陣列」以「新元素」。
    乃得「陣列」。
是謂「陣列追加」之術也。

注曰「===== 開始測試 ===== 」
書之「列經核心函數測試開始」。

注曰「測試1：基本陣列創建與長度」
書之「測試1：基本陣列創建與長度」。
吾有一列。名之曰「測試陣列」。
充「測試陣列」以一。以二。以三。

吾有一數。名之曰「初始長度」。
施「取陣列長度」於「測試陣列」。名之曰「初始長度」。
書之「初始長度：」。
書之「初始長度」。

注曰「測試2：元素存取」
書之「測試2：元素存取」。
吾有一數。名之曰「第一元素」。
夫「測試陣列」。夫一。取二以施「取陣列元素」。名之曰「第一元素」。
書之「第一個元素：」。
書之「第一元素」。

吾有一數。名之曰「第三元素」。
夫「測試陣列」。夫三。取二以施「取陣列元素」。名之曰「第三元素」。
書之「第三個元素：」。
書之「第三元素」。

注曰「測試3：陣列追加」
書之「測試3：陣列追加」。
吾有一列。名之曰「擴展陣列」。
夫「測試陣列」。夫四。取二以施「陣列追加」。名之曰「擴展陣列」。

吾有一數。名之曰「新長度」。
施「取陣列長度」於「擴展陣列」。名之曰「新長度」。
書之「追加後長度：」。
書之「新長度」。

吾有一數。名之曰「最後元素」。
夫「擴展陣列」。夫四。取二以施「取陣列元素」。名之曰「最後元素」。
書之「最後元素：」。
書之「最後元素」。

書之「所有測試完成」。