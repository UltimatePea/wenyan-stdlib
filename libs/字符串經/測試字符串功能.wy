/* 字符串經測試文件 - 基礎功能測試 */
/* Author: Whisky, PR Worker */

吾嘗觀「「字符串經」」之書。方悟「取字符串長度」「拼接字符串」「比較字符串」「字符串為空」「字符串包含」「字符串轉大寫」「字符串轉小寫」「重複字符串」之義。

書之「「===== 測試字符串長度 =====」」。
吾有一言。名之曰「測試字符串」。
昔之「測試字符串」者。今「「你好世界」」是矣。

吾有一數。名之曰「長度」。
施「取字符串長度」於「測試字符串」。名之曰「長度」。
書之「「字符串長度為：」」。書之「長度」。

書之「「===== 測試字符串拼接 =====」」。
吾有一言。名之曰「字符串甲」。
昔之「字符串甲」者。今「「古典」」是矣。
吾有一言。名之曰「字符串乙」。
昔之「字符串乙」者。今「「程序設計」」是矣。
吾有一言。名之曰「拼接結果」。
施「拼接字符串」於「字符串甲」於「字符串乙」。名之曰「拼接結果」。
書之「「拼接結果：」」。書之「拼接結果」。

書之「「===== 測試字符串比較 =====」」。
吾有一言。名之曰「字符串一」。
昔之「字符串一」者。今「「甲」」是矣。
吾有一言。名之曰「字符串二」。
昔之「字符串二」者。今「「乙」」是矣。
吾有一數。名之曰「比較結果」。
施「比較字符串」於「字符串一」於「字符串二」。名之曰「比較結果」。
書之「「比較結果：」」。書之「比較結果」。

書之「「===== 測試空字符串判斷 =====」」。
吾有一言。名之曰「空字符串」。
昔之「空字符串」者。今「「」」是矣。
吾有一爻。名之曰「是否為空」。
施「字符串為空」於「空字符串」。名之曰「是否為空」。
書之「「空字符串是否為空：」」。書之「是否為空」。

吾有一言。名之曰「非空字符串」。
昔之「非空字符串」者。今「「文」」是矣。
施「字符串為空」於「非空字符串」。名之曰「是否為空」。
書之「「非空字符串是否為空：」」。書之「是否為空」。

書之「「===== 測試字符串包含 =====」」。
吾有一言。名之曰「主字符串」。
昔之「主字符串」者。今「「文言」」是矣。
吾有一言。名之曰「子字符串」。
昔之「子字符串」者。今「「文言」」是矣。
吾有一爻。名之曰「包含結果」。
施「字符串包含」於「主字符串」於「子字符串」。名之曰「包含結果」。
書之「「字符串包含結果：」」。書之「包含結果」。

書之「「===== 測試重複字符串 =====」」。
吾有一言。名之曰「基礎字符串」。
昔之「基礎字符串」者。今「「福」」是矣。
吾有一言。名之曰「重複結果」。
施「重複字符串」於「基礎字符串」於三。名之曰「重複結果」。
書之「「重複結果：」」。書之「重複結果」。

書之「「===== 測試字符串轉換 =====」」。
吾有一言。名之曰「原字符串」。
昔之「原字符串」者。今「「測試字符串」」是矣。
吾有一言。名之曰「大寫結果」。
施「字符串轉大寫」於「原字符串」。名之曰「大寫結果」。
書之「「轉大寫結果：」」。書之「大寫結果」。

吾有一言。名之曰「小寫結果」。
施「字符串轉小寫」於「原字符串」。名之曰「小寫結果」。
書之「「轉小寫結果：」」。書之「小寫結果」。

書之「「===== 字符串經測試完成 =====」」。