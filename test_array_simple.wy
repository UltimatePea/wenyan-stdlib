注曰「Simple Array Function Tests」
注曰「Author: Whisky, PR Worker」

注曰「Create array and test basic operations」
吾有一列。名之曰「測試陣列」。
充「測試陣列」以一。
充「測試陣列」以二。
充「測試陣列」以三。

注曰「Test array length」
吾有一數。名之曰「長度」。
夫「測試陣列」之長。名之曰「長度」。
書之「Length:」。書之「長度」。

注曰「Test array element access」
吾有一物。名之曰「元素」。
夫「測試陣列」之一。名之曰「元素」。
書之「First element:」。書之「元素」。

夫「測試陣列」之二。名之曰「元素」。
書之「Second element:」。書之「元素」。

注曰「Test array sum」
吾有一數。名之曰「總和」。
昔之「總和」者。今〇是矣。
吾有一數。名之曰「索引」。
昔之「索引」者。今一是矣。

恆為是。
    若「索引」大於「長度」者。乃止。云云。
    吾有一數。名之曰「當前元素」。
    夫「測試陣列」之「索引」。名之曰「當前元素」。
    加「總和」以「當前元素」。昔之「總和」者。今其是矣。
    加「索引」以一。昔之「索引」者。今其是矣。
云云。

書之「Sum:」。書之「總和」。

注曰「All tests complete」