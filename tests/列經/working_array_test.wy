注曰「陣列庫工作測試 - Working Array Library Test」
注曰「Author: Whisky, PR Worker」

注曰「創建測試陣列」
吾有一列。名之曰「測試陣列」。
充「測試陣列」以一。
充「測試陣列」以二。
充「測試陣列」以三。

注曰「測試陣列長度函數」
吾有一術。名之曰「取陣列長度」。欲行是術。必先得一物。曰「陣列」。乃行是術曰。
    吾有一數。名之曰「長度值」。
    夫「陣列」之長。名之曰「長度值」。
    乃得「長度值」。
是謂「取陣列長度」之術也。

吾有一數。名之曰「長度結果」。
施「取陣列長度」於「測試陣列」。名之曰「長度結果」。

注曰「測試陣列元素存取函數」
吾有一術。名之曰「取陣列元素」。欲行是術。必先得二物。曰「陣列」。曰「索引」。乃行是術曰。
    吾有一物。名之曰「元素值」。
    夫「陣列」之「索引」。名之曰「元素值」。
    乃得「元素值」。
是謂「取陣列元素」之術也。

吾有一數。名之曰「第一元素」。
施「取陣列元素」於「測試陣列」於一。名之曰「第一元素」。

吾有一數。名之曰「第二元素」。
施「取陣列元素」於「測試陣列」於二。名之曰「第二元素」。

注曰「測試陣列追加函數」
吾有一術。名之曰「陣列追加」。欲行是術。必先得二物。曰「陣列」。曰「新元素」。乃行是術曰。
    充「陣列」以「新元素」。
    乃得「陣列」。
是謂「陣列追加」之術也。

施「陣列追加」於「測試陣列」於四。名之曰「測試陣列」。

吾有一數。名之曰「新長度」。
施「取陣列長度」於「測試陣列」。名之曰「新長度」。

注曰「測試陣列求和函數」
吾有一術。名之曰「陣列求和」。欲行是術。必先得一物。曰「陣列」。乃行是術曰。
    吾有一數。名之曰「總和」。
    吾有一數。名之曰「索引」。
    吾有一數。名之曰「長度」。
    
    夫「陣列」之長。名之曰「長度」。
    昔之「總和」者。今〇是矣。
    昔之「索引」者。今一是矣。
    
    恆為是。
        若「索引」大於「長度」者。乃止。云云。
        吾有一數。名之曰「當前元素」。
        夫「陣列」之「索引」。名之曰「當前元素」。
        加「總和」以「當前元素」。昔之「總和」者。今其是矣。
        加「索引」以一。昔之「索引」者。今其是矣。
    云云。
    
    乃得「總和」。
是謂「陣列求和」之術也。

吾有一數。名之曰「總和結果」。
施「陣列求和」於「測試陣列」。名之曰「總和結果」。

注曰「輸出測試結果 - 使用JavaScript輸出」