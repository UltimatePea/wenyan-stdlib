注曰「「檔經測試套件 - IO Library Test Suite」」。
注曰「「Author: Whisky, PR Worker」」。
注曰「「Version: 1.0」」。
注曰「「Created: 2025-08-03」」。

注曰「「引入依賴項」」。

吾有一術。名之曰「驗證操作結果」。欲行是術。必先得三言。曰「實際結果」。曰「期望結果」。曰「測試描述」。乃行是術曰。
    若「實際結果」等於「期望結果」者。
        書之「✅ 通過：」。書之「測試描述」。
        乃得陽。
    云云。
    
    書之「❌ 失敗：」。書之「測試描述」。
    書之「   期望：」。書之「期望結果」。
    書之「   實際：」。書之「實際結果」。
    乃得陰。
是謂「驗證操作結果」之術也。

吾有一術。名之曰「驗證文件內容」。欲行是術。必先得三言。曰「文件路徑」。曰「期望內容」。曰「測試描述」。乃行是術曰。
    施「讀取文件」於「文件路徑」。名之曰「實際內容」。
    
    若「實際內容」等於「期望內容」者。
        書之「✅ 通過：」。書之「測試描述」。
        乃得陽。
    云云。
    
    書之「❌ 失敗：」。書之「測試描述」。
    書之「   期望內容：」。書之「期望內容」。
    書之「   實際內容：」。書之「實際內容」。
    乃得陰。
是謂「驗證文件內容」之術也。

吾有一術。名之曰「計算通過率」。欲行是術。必先得二數。曰「通過數量」。曰「總測試數量」。乃行是術曰。
    若「總測試數量」等於〇者。
        乃得〇。
    云云。
    
    乘「通過數量」以一〇〇。名之曰「百分比分子」。
    除「百分比分子」以「總測試數量」。乃得其也。
是謂「計算通過率」之術也。

注曰「「測試統計變量 Test Statistics Variables」」。

吾有一數。名之曰「總測試數」。
昔之「總測試數」者。今〇是矣。

吾有一數。名之曰「通過測試數」。
昔之「通過測試數」者。今〇是矣。

吾有一數。名之曰「失敗測試數」。
昔之「失敗測試數」者。今〇是矣。

注曰「「主測試套件開始 Main Test Suite Start」」。

書之「」。
書之「================================================」。
書之「檔經測試套件 - IO Library Test Suite」。
書之「Author: Whisky, PR Worker」。
書之「Date: 2025-08-03」。
書之「================================================」。
書之「」。

注曰「「文件操作測試 File Operations Tests」」。

書之「🧪 開始文件操作測試 Starting File Operations Tests」。
書之「─────────────────────────────────────」。

注曰「「測試1: 文件寫入和讀取」」。
書之「」。
書之「測試 1: 文件寫入和讀取 Write and Read File」。

吾有一言。名之曰「測試文件1」。
昔之「測試文件1」者。今「「test_write_read.txt」」是矣。

吾有一言。名之曰「測試內容1」。
昔之「測試內容1」者。今「「這是測試內容\n支持中文字符\n第三行內容」」是矣。

加「總測試數」以一。昔之「總測試數」者。今其是矣。
施「寫入文件」於「測試文件1」於「測試內容1」。名之曰「寫入結果1」。
施「驗證操作結果」於「寫入結果1」於「操作成功」於「文件寫入操作」。名之曰「寫入測試結果」。

若「寫入測試結果」等於陽者。
    加「通過測試數」以一。昔之「通過測試數」者。今其是矣。
云云。
若「寫入測試結果」等於陰者。
    加「失敗測試數」以一。昔之「失敗測試數」者。今其是矣。
云云。

加「總測試數」以一。昔之「總測試數」者。今其是矣。
施「讀取文件」於「測試文件1」。名之曰「讀取內容1」。
施「驗證文件內容」於「測試文件1」於「測試內容1」於「文件讀取操作」。名之曰「讀取測試結果」。

若「讀取測試結果」等於陽者。
    加「通過測試數」以一。昔之「通過測試數」者。今其是矣。
云云。
若「讀取測試結果」等於陰者。
    加「失敗測試數」以一。昔之「失敗測試數」者。今其是矣。
云云。

注曰「「測試2: 文件存在性檢查」」。
書之「」。
書之「測試 2: 文件存在性檢查 File Existence Check」。

加「總測試數」以一。昔之「總測試數」者。今其是矣。
施「檢查文件存在」於「測試文件1」。名之曰「存在檢查結果」。
施「驗證操作結果」於「存在檢查結果」於陽於「存在文件的檢查」。名之曰「存在檢查測試結果」。

若「存在檢查測試結果」等於陽者。
    加「通過測試數」以一。昔之「通過測試數」者。今其是矣。
云云。
若「存在檢查測試結果」等於陰者。
    加「失敗測試數」以一。昔之「失敗測試數」者。今其是矣。
云云。

加「總測試數」以一。昔之「總測試數」者。今其是矣。
施「檢查文件存在」於「「不存在的文件.txt」」。名之曰「不存在檢查結果」。
施「驗證操作結果」於「不存在檢查結果」於陰於「不存在文件的檢查」。名之曰「不存在檢查測試結果」。

若「不存在檢查測試結果」等於陽者。
    加「通過測試數」以一。昔之「通過測試數」者。今其是矣。
云云。
若「不存在檢查測試結果」等於陰者。
    加「失敗測試數」以一。昔之「失敗測試數」者。今其是矣。
云云。

注曰「「測試3: 文件信息獲取」」。
書之「」。
書之「測試 3: 文件信息獲取 File Information Retrieval」。

加「總測試數」以一。昔之「總測試數」者。今其是矣。
施「獲取文件信息」於「測試文件1」。名之曰「文件信息結果」。
注曰「「檢查返回的是列表且不為空」」。
夫「文件信息結果」之長。名之曰「信息長度」。

若「信息長度」大於〇者。
    書之「✅ 通過：文件信息獲取」。
    加「通過測試數」以一。昔之「通過測試數」者。今其是矣。
云云。
若「信息長度」等於〇者。
    書之「❌ 失敗：文件信息獲取 - 返回空信息」。
    加「失敗測試數」以一。昔之「失敗測試數」者。今其是矣。
云云。

注曰「「測試4: 文件大小獲取」」。
書之「」。
書之「測試 4: 文件大小獲取 File Size Retrieval」。

加「總測試數」以一。昔之「總測試數」者。今其是矣。
施「獲取文件大小」於「測試文件1」。名之曰「文件大小結果」。

若「文件大小結果」大於〇者。
    書之「✅ 通過：文件大小獲取 - 大小：」。書之「文件大小結果」。
    加「通過測試數」以一。昔之「通過測試數」者。今其是矣。
云云。
若「文件大小結果」小於一者。
    書之「❌ 失敗：文件大小獲取 - 無效大小：」。書之「文件大小結果」。
    加「失敗測試數」以一。昔之「失敗測試數」者。今其是矣。
云云。

注曰「「測試5: 文件複製」」。
書之「」。
書之「測試 5: 文件複製 File Copy」。

吾有一言。名之曰「複製目標文件」。
昔之「複製目標文件」者。今「「test_copy.txt」」是矣。

加「總測試數」以一。昔之「總測試數」者。今其是矣。
施「複製文件」於「測試文件1」於「複製目標文件」。名之曰「複製結果」。
施「驗證操作結果」於「複製結果」於「操作成功」於「文件複製操作」。名之曰「複製測試結果」。

若「複製測試結果」等於陽者。
    加「通過測試數」以一。昔之「通過測試數」者。今其是矣。
云云。
若「複製測試結果」等於陰者。
    加「失敗測試數」以一。昔之「失敗測試數」者。今其是矣。
云云。

注曰「「測試6: 文件重命名」」。
書之「」。
書之「測試 6: 文件重命名 File Rename」。

吾有一言。名之曰「重命名目標文件」。
昔之「重命名目標文件」者。今「「test_renamed.txt」」是矣。

加「總測試數」以一。昔之「總測試數」者。今其是矣。
施「重命名文件」於「複製目標文件」於「重命名目標文件」。名之曰「重命名結果」。
施「驗證操作結果」於「重命名結果」於「操作成功」於「文件重命名操作」。名之曰「重命名測試結果」。

若「重命名測試結果」等於陽者。
    加「通過測試數」以一。昔之「通過測試數」者。今其是矣。
云云。
若「重命名測試結果」等於陰者。
    加「失敗測試數」以一。昔之「失敗測試數」者。今其是矣。
云云。

注曰「「測試7: 文件追加」」。
書之「」。
書之「測試 7: 文件追加 File Append」。

吾有一言。名之曰「追加內容」。
昔之「追加內容」者。今「「\n這是追加的內容\n新的一行」」是矣。

加「總測試數」以一。昔之「總測試數」者。今其是矣。
施「追加文件」於「測試文件1」於「追加內容」。名之曰「追加結果」。
施「驗證操作結果」於「追加結果」於「操作成功」於「文件追加操作」。名之曰「追加測試結果」。

若「追加測試結果」等於陽者。
    加「通過測試數」以一。昔之「通過測試數」者。今其是矣。
云云。
若「追加測試結果」等於陰者。
    加「失敗測試數」以一。昔之「失敗測試數」者。今其是矣。
云云。

書之「📋 文件操作測試完成」。

注曰「「目錄操作測試 Directory Operations Tests」」。

書之「」。
書之「🧪 開始目錄操作測試 Starting Directory Operations Tests」。
書之「─────────────────────────────────────」。

注曰「「測試8: 目錄創建」」。
書之「」。
書之「測試 8: 目錄創建 Directory Creation」。

吾有一言。名之曰「測試目錄1」。
昔之「測試目錄1」者。今「「test_directory」」是矣。

加「總測試數」以一。昔之「總測試數」者。今其是矣。
施「創建目錄」於「測試目錄1」。名之曰「創建目錄結果」。
施「驗證操作結果」於「創建目錄結果」於「操作成功」於「目錄創建操作」。名之曰「創建目錄測試結果」。

若「創建目錄測試結果」等於陽者。
    加「通過測試數」以一。昔之「通過測試數」者。今其是矣。
云云。
若「創建目錄測試結果」等於陰者。
    加「失敗測試數」以一。昔之「失敗測試數」者。今其是矣。
云云。

注曰「「測試9: 目錄存在性檢查」」。
書之「」。
書之「測試 9: 目錄存在性檢查 Directory Existence Check」。

加「總測試數」以一。昔之「總測試數」者。今其是矣。
施「檢查目錄存在」於「測試目錄1」。名之曰「目錄存在結果」。
施「驗證操作結果」於「目錄存在結果」於陽於「目錄存在性檢查」。名之曰「目錄存在測試結果」。

若「目錄存在測試結果」等於陽者。
    加「通過測試數」以一。昔之「通過測試數」者。今其是矣。
云云。
若「目錄存在測試結果」等於陰者。
    加「失敗測試數」以一。昔之「失敗測試數」者。今其是矣。
云云。

注曰「「測試10: 目錄內容列出」」。
書之「」。
書之「測試 10: 目錄內容列出 Directory Contents Listing」。

加「總測試數」以一。昔之「總測試數」者。今其是矣。
施「列出目錄」於「測試目錄1」。名之曰「目錄內容結果」。
夫「目錄內容結果」之長。名之曰「內容數量」。

若「內容數量」大於負一者。  注曰「「至少不是錯誤」」。
    書之「✅ 通過：目錄內容列出 - 項目數：」。書之「內容數量」。
    加「通過測試數」以一。昔之「通過測試數」者。今其是矣。
云云。
若「內容數量」等於負一者。
    書之「❌ 失敗：目錄內容列出 - 無法列出內容」。
    加「失敗測試數」以一。昔之「失敗測試數」者。今其是矣。
云云。

書之「📁 目錄操作測試完成」。

注曰「「路徑處理測試 Path Handling Tests」」。

書之「」。
書之「🧪 開始路徑處理測試 Starting Path Handling Tests」。
書之「─────────────────────────────────────」。

注曰「「測試11: 路徑連接」」。
書之「」。
書之「測試 11: 路徑連接 Path Joining」。

吾有一言。名之曰「路徑部分1」。
昔之「路徑部分1」者。今「「/home/user」」是矣。

吾有一言。名之曰「路徑部分2」。
昔之「路徑部分2」者。今「「documents」」是矣。

吾有一言。名之曰「期望路徑」。
昔之「期望路徑」者。今「「/home/user/documents」」是矣。

加「總測試數」以一。昔之「總測試數」者。今其是矣。
施「連接路徑」於「路徑部分1」於「路徑部分2」。名之曰「連接路徑結果」。
施「驗證操作結果」於「連接路徑結果」於「期望路徑」於「路徑連接操作」。名之曰「路徑連接測試結果」。

若「路徑連接測試結果」等於陽者。
    加「通過測試數」以一。昔之「通過測試數」者。今其是矣。
云云。
若「路徑連接測試結果」等於陰者。
    加「失敗測試數」以一。昔之「失敗測試數」者。今其是矣。
云云。

注曰「「測試12: 文件擴展名獲取」」。
書之「」。
書之「測試 12: 文件擴展名獲取 File Extension Extraction」。

加「總測試數」以一。昔之「總測試數」者。今其是矣。
施「獲取文件擴展名」於「「test.txt」」。名之曰「擴展名結果1」。
施「驗證操作結果」於「擴展名結果1」於「「.txt」」於「txt文件擴展名獲取」。名之曰「擴展名測試結果1」。

若「擴展名測試結果1」等於陽者。
    加「通過測試數」以一。昔之「通過測試數」者。今其是矣。
云云。
若「擴展名測試結果1」等於陰者。
    加「失敗測試數」以一。昔之「失敗測試數」者。今其是矣。
云云。

加「總測試數」以一。昔之「總測試數」者。今其是矣。
施「獲取文件擴展名」於「「data.json」」。名之曰「擴展名結果2」。
施「驗證操作結果」於「擴展名結果2」於「「.json」」於「json文件擴展名獲取」。名之曰「擴展名測試結果2」。

若「擴展名測試結果2」等於陽者。
    加「通過測試數」以一。昔之「通過測試數」者。今其是矣。
云云。
若「擴展名測試結果2」等於陰者。
    加「失敗測試數」以一。昔之「失敗測試數」者。今其是矣。
云云。

注曰「「測試13: 路徑驗證」」。
書之「」。
書之「測試 13: 路徑驗證 Path Validation」。

加「總測試數」以一。昔之「總測試數」者。今其是矣。
施「驗證文件路徑」於「「valid/path/file.txt」」。名之曰「路徑驗證結果1」。
施「驗證操作結果」於「路徑驗證結果1」於陽於「有效路徑驗證」。名之曰「路徑驗證測試結果1」。

若「路徑驗證測試結果1」等於陽者。
    加「通過測試數」以一。昔之「通過測試數」者。今其是矣。
云云。
若「路徑驗證測試結果1」等於陰者。
    加「失敗測試數」以一。昔之「失敗測試數」者。今其是矣。
云云。

加「總測試數」以一。昔之「總測試數」者。今其是矣。
施「驗證文件路徑」於「「」」。名之曰「路徑驗證結果2」。
施「驗證操作結果」於「路徑驗證結果2」於陰於「空路徑驗證」。名之曰「路徑驗證測試結果2」。

若「路徑驗證測試結果2」等於陽者。
    加「通過測試數」以一。昔之「通過測試數」者。今其是矣。
云云。
若「路徑驗證測試結果2」等於陰者。
    加「失敗測試數」以一。昔之「失敗測試數」者。今其是矣。
云云。

書之「🛤️  路徑處理測試完成」。

注曰「「編碼操作測試 Encoding Operations Tests」」。

書之「」。
書之「🧪 開始編碼操作測試 Starting Encoding Operations Tests」。
書之「─────────────────────────────────────」。

注曰「「UTF-8編碼文件讀取」」。
書之「」。
書之「測試 14: UTF-8編碼文件讀取 UTF-8 Encoded File Reading」。

吾有一言。名之曰「UTF8測試文件」。
昔之「UTF8測試文件」者。今「「utf8_test.txt」」是矣。

吾有一言。名之曰「UTF8測試內容」。
昔之「UTF8測試內容」者。今「「UTF-8編碼測試\n支持中文字符\n測試完成」」是矣。

注曰「「先寫入UTF-8文件」」。
施「帶編碼寫入文件」於「UTF8測試文件」於「UTF8測試內容」於「編碼UTF8」。名之曰「UTF8寫入結果」。

加「總測試數」以一。昔之「總測試數」者。今其是矣。
施「帶編碼讀取文件」於「UTF8測試文件」於「編碼UTF8」。名之曰「UTF8讀取結果」。

注曰「「檢查讀取結果不是錯誤信息」」。
若「UTF8讀取結果」不等於「「文件不存在錯誤」」者。
    書之「✅ 通過：UTF-8編碼文件讀取」。
    加「通過測試數」以一。昔之「通過測試數」者。今其是矣。
云云。
若「UTF8讀取結果」等於「「文件不存在錯誤」」者。
    書之「❌ 失敗：UTF-8編碼文件讀取」。
    加「失敗測試數」以一。昔之「失敗測試數」者。今其是矣。
云云。

注曰「「測試15: GBK編碼支持」」。
書之「」。
書之「測試 15: GBK編碼支持 GBK Encoding Support」。

吾有一言。名之曰「GBK測試文件」。
昔之「GBK測試文件」者。今「「gbk_test.txt」」是矣。

吾有一言。名之曰「GBK測試內容」。
昔之「GBK測試內容」者。今「「GBK編碼測試內容」」是矣。

加「總測試數」以一。昔之「總測試數」者。今其是矣。
施「帶編碼寫入文件」於「GBK測試文件」於「GBK測試內容」於「編碼GBK」。名之曰「GBK寫入結果」。
施「驗證操作結果」於「GBK寫入結果」於「操作成功」於「GBK編碼文件寫入」。名之曰「GBK測試結果」。

若「GBK測試結果」等於陽者。
    加「通過測試數」以一。昔之「通過測試數」者。今其是矣。
云云。
若「GBK測試結果」等於陰者。
    加「失敗測試數」以一。昔之「失敗測試數」者。今其是矣。
云云。

書之「🔤 編碼操作測試完成」。

注曰「「控制台交互測試 Console Interaction Tests」」。

書之「」。
書之「🧪 開始控制台交互測試 Starting Console Interaction Tests」。
書之「─────────────────────────────────────」。

注曰「「測試16: 控制台輸出」」。
書之「」。
書之「測試 16: 控制台輸出 Console Output」。

加「總測試數」以一。昔之「總測試數」者。今其是矣。
施「控制台輸出」於「「測試控制台輸出」」。名之曰「控制台輸出結果」。
施「驗證操作結果」於「控制台輸出結果」於「操作成功」於「控制台輸出操作」。名之曰「控制台輸出測試結果」。

若「控制台輸出測試結果」等於陽者。
    加「通過測試數」以一。昔之「通過測試數」者。今其是矣。
云云。
若「控制台輸出測試結果」等於陰者。
    加「失敗測試數」以一。昔之「失敗測試數」者。今其是矣。
云云。

注曰「「測試17: 格式化輸出」」。
書之「」。
書之「測試 17: 格式化輸出 Formatted Output」。

加「總測試數」以一。昔之「總測試數」者。今其是矣。
施「格式化輸出」於「「測試項目」」於「「測試值」」。名之曰「格式化輸出結果」。
施「驗證操作結果」於「格式化輸出結果」於「操作成功」於「格式化輸出操作」。名之曰「格式化輸出測試結果」。

若「格式化輸出測試結果」等於陽者。
    加「通過測試數」以一。昔之「通過測試數」者。今其是矣。
云云。
若「格式化輸出測試結果」等於陰者。
    加「失敗測試數」以一。昔之「失敗測試數」者。今其是矣。
云云。

書之「💬 控制台交互測試完成」。

注曰「「錯誤處理測試 Error Handling Tests」」。

書之「」。
書之「🧪 開始錯誤處理測試 Starting Error Handling Tests」。
書之「─────────────────────────────────────」。

注曰「「測試18: 文件不存在錯誤」」。
書之「」。
書之「測試 18: 文件不存在錯誤 File Not Found Error」。

加「總測試數」以一。昔之「總測試數」者。今其是矣。
施「讀取文件」於「「絕對不存在的文件.txt」」。名之曰「不存在文件讀取結果」。

注曰「「檢查是否返回了錯誤信息而不是正常內容」」。
若「不存在文件讀取結果」等於「「示例文件內容\n這是第二行\n文件讀取成功」」者。
    書之「❌ 失敗：文件不存在錯誤處理 - 應該返回錯誤」。
    加「失敗測試數」以一。昔之「失敗測試數」者。今其是矣。
云云。
若「不存在文件讀取結果」不等於「「示例文件內容\n這是第二行\n文件讀取成功」」者。
    書之「✅ 通過：文件不存在錯誤處理」。
    加「通過測試數」以一。昔之「通過測試數」者。今其是矣。
云云。

注曰「「測試19: 無效路徑錯誤」」。
書之「」。
書之「測試 19: 無效路徑錯誤 Invalid Path Error」。

加「總測試數」以一。昔之「總測試數」者。今其是矣。
施「驗證文件路徑」於「「」」。名之曰「無效路徑驗證結果」。
施「驗證操作結果」於「無效路徑驗證結果」於陰於「空路徑驗證錯誤處理」。名之曰「無效路徑測試結果」。

若「無效路徑測試結果」等於陽者。
    加「通過測試數」以一。昔之「通過測試數」者。今其是矣。
云云。
若「無效路徑測試結果」等於陰者。
    加「失敗測試數」以一。昔之「失敗測試數」者。今其是矣。
云云。

注曰「「測試20: 不支持的編碼錯誤」」。
書之「」。
書之「測試 20: 不支持的編碼錯誤 Unsupported Encoding Error」。

加「總測試數」以一。昔之「總測試數」者。今其是矣。
施「帶編碼讀取文件」於「測試文件1」於「「INVALID_ENCODING」」。名之曰「無效編碼結果」。

注曰「「檢查是否返回了錯誤信息」」。
若「無效編碼結果」等於「「使用特定編碼讀取的文件內容」」者。
    書之「❌ 失敗：無效編碼錯誤處理 - 應該返回錯誤」。
    加「失敗測試數」以一。昔之「失敗測試數」者。今其是矣。
云云。
若「無效編碼結果」不等於「「使用特定編碼讀取的文件內容」」者。
    書之「✅ 通過：無效編碼錯誤處理」。
    加「通過測試數」以一。昔之「通過測試數」者。今其是矣。
云云。

書之「⚠️  錯誤處理測試完成」。

注曰「「流式操作測試 Stream Operations Tests」」。

書之「」。
書之「🧪 開始流式操作測試 Starting Stream Operations Tests」。
書之「─────────────────────────────────────」。

注曰「「測試21: 流式文件讀取」」。
書之「」。
書之「測試 21: 流式文件讀取 Stream File Reading」。

加「總測試數」以一。昔之「總測試數」者。今其是矣。
施「流式讀取文件」於「測試文件1」於一〇二四。名之曰「流式讀取結果」。
夫「流式讀取結果」之長。名之曰「數據塊數量」。

若「數據塊數量」大於〇者。
    書之「✅ 通過：流式文件讀取 - 數據塊數：」。書之「數據塊數量」。
    加「通過測試數」以一。昔之「通過測試數」者。今其是矣。
云云。
若「數據塊數量」等於〇者。
    書之「❌ 失敗：流式文件讀取 - 無數據塊」。
    加「失敗測試數」以一。昔之「失敗測試數」者。今其是矣。
云云。

書之「🌊 流式操作測試完成」。

注曰「「測試結果統計 Test Results Summary」」。

書之「」。
書之「================================================」。
書之「測試結果統計 Test Results Summary」。
書之「================================================」。

書之「總測試數 Total Tests: 」。書之「總測試數」。
書之「通過測試數 Passed Tests: 」。書之「通過測試數」。
書之「失敗測試數 Failed Tests: 」。書之「失敗測試數」。

施「計算通過率」於「通過測試數」於「總測試數」。名之曰「通過率」。
書之「通過率 Pass Rate: 」。書之「通過率」。書之「%」。

書之「」。
若「失敗測試數」等於〇者。
    書之「🎉 所有測試通過！All Tests Passed!」。
    書之「✅ 檔經IO庫功能完全正常」。
    書之「✅ IO Library functionality is fully operational」。
云云。
若「失敗測試數」大於〇者。
    書之「⚠️  有」。書之「失敗測試數」。書之「個測試失敗」。
    書之「⚠️  」。書之「失敗測試數」。書之「 tests failed」。
    書之「🔧 需要修復失敗的測試項目」。
    書之「🔧 Failed test items need to be fixed」。
云云。

書之「」。
書之「檔經測試套件執行完成 IO Library Test Suite Completed」。
書之「Author: Whisky, PR Worker」。
書之「Date: 2025-08-03」。
書之「================================================」。
書之「」。