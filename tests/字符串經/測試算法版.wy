注曰「測試算法版字符串經」
注曰「Author: Whisky, PR Worker」
注曰「驗證真正的算法實現而非模式匹配」

注曰「導入算法版函數」
吾嘗觀「「../../libs/字符串經/字符串經.wy」」之書。方悟「字符串經」之義。

書之「===== 算法版字符串經測試 =====」。

/* 測試字符串反轉算法 */
書之「=== 測試字符串反轉算法 ===」。

書之「測試1：二字符反轉」。
吾有一言。名之曰「測試字符串」。
昔之「測試字符串」者。今「「xy」」是矣。
施「字符串反轉」於「測試字符串」。名之曰「反轉結果」。
書之「輸入：xy」。
書之「輸出：」。
書之「反轉結果」。

書之「測試2：三字符反轉」。
昔之「測試字符串」者。今「「abc」」是矣。
施「字符串反轉」於「測試字符串」。名之曰「反轉結果」。
書之「輸入：abc」。
書之「輸出：」。
書之「反轉結果」。

書之「測試3：四字符反轉」。
昔之「測試字符串」者。今「「test」」是矣。
施「字符串反轉」於「測試字符串」。名之曰「反轉結果」。
書之「輸入：test」。
書之「輸出：」。
書之「反轉結果」。

書之「測試4：五字符反轉」。
昔之「測試字符串」者。今「「hello」」是矣。
施「字符串反轉」於「測試字符串」。名之曰「反轉結果」。
書之「輸入：hello」。
書之「輸出：」。
書之「反轉結果」。

/* 測試字符串搜索算法 */
書之「=== 測試字符串搜索算法 ===」。

書之「測試5：單字符搜索」。
昔之「測試字符串」者。今「「hello」」是矣。
施「在文字中尋找」於「測試字符串」於「「e」」。名之曰「搜索位置」。
書之「在hello中尋找e的位置：」。
書之「搜索位置」。

書之「測試6：首字符搜索」。
施「在文字中尋找」於「測試字符串」於「「h」」。名之曰「搜索位置」。
書之「在hello中尋找h的位置：」。
書之「搜索位置」。

書之「測試7：末字符搜索」。
施「在文字中尋找」於「測試字符串」於「「o」」。名之曰「搜索位置」。
書之「在hello中尋找o的位置：」。
書之「搜索位置」。

書之「測試8：不存在的字符」。
施「在文字中尋找」於「測試字符串」於「「x」」。名之曰「搜索位置」。
書之「在hello中尋找x的位置：」。
書之「搜索位置」。

/* 測試字符提取算法 */
書之「=== 測試字符提取算法 ===」。

書之「測試9：提取各位置字符」。
昔之「測試字符串」者。今「「hello」」是矣。
施「取字符」於「測試字符串」於一。名之曰「字符」。
書之「hello第1個字符：」。
書之「字符」。

施「取字符」於「測試字符串」於三。名之曰「字符」。
書之「hello第3個字符：」。
書之「字符」。

施「取字符」於「測試字符串」於五。名之曰「字符」。
書之「hello第5個字符：」。
書之「字符」。

/* 測試字符串截取算法 */
書之「=== 測試字符串截取算法 ===」。

書之「測試10：截取子字符串」。
昔之「測試字符串」者。今「「hello」」是矣。
施「字符串截取」於「測試字符串」於一於三。名之曰「截取結果」。
書之「hello截取1-3：」。
書之「截取結果」。

施「字符串截取」於「測試字符串」於二於二。名之曰「截取結果」。
書之「hello截取2-2：」。
書之「截取結果」。

/* 測試數字驗證算法 */
書之「=== 測試數字驗證算法 ===」。

書之「測試11：單數字驗證」。
昔之「測試字符串」者。今「「5」」是矣。
施「是否純數字」於「測試字符串」。名之曰「是否數字」。
書之「5是否為純數字：」。
書之「是否數字」。

書之「測試12：雙數字驗證」。
昔之「測試字符串」者。今「「42」」是矣。
施「是否純數字」於「測試字符串」。名之曰「是否數字」。
書之「42是否為純數字：」。
書之「是否數字」。

書之「測試13：非數字驗證」。
昔之「測試字符串」者。今「「a5」」是矣。
施「是否純數字」於「測試字符串」。名之曰「是否數字」。
書之「a5是否為純數字：」。
書之「是否數字」。

注曰「測試新增功能」
書之「=== 測試新增功能 ===」。

書之「測試14：字符串替換算法」。
昔之「測試字符串」者。今「「hello world hello」」是矣。
施「字符串替換」於「測試字符串」於「「hello」」於「「hi」」。名之曰「替換結果」。
書之「將hello替換為hi：」。
書之「替換結果」。

書之「測試15：字符串去空白算法」。
昔之「測試字符串」者。今「「  test string  」」是矣。
施「字符串去空白」於「測試字符串」。名之曰「去空白結果」。
書之「去空白前：」。
書之「測試字符串」。
書之「去空白後：」。
書之「去空白結果」。

書之「測試16：大小寫轉換算法」。
昔之「測試字符串」者。今「「Hello World」」是矣。
施「字符串轉大寫」於「測試字符串」。名之曰「大寫結果」。
書之「轉大寫：」。
書之「大寫結果」。

施「字符串轉小寫」於「大寫結果」。名之曰「小寫結果」。
書之「轉小寫：」。
書之「小寫結果」。

書之「測試17：字符串劃割算法」。
昔之「測試字符串」者。今「「a,b,c,d」」是矣。
施「字符串劃割」於「測試字符串」於「「,」」。名之曰「分割數量」。
書之「a,b,c,d按逗號分割的數量：」。
書之「分割數量」。

書之「測試18：字符驗證算法」。
昔之「測試字符串」者。今「「abcdef」」是矣。
施「是否純字母」於「測試字符串」。名之曰「是否字母」。
書之「abcdef是否純字母：」。
書之「是否字母」。

昔之「測試字符串」者。今「「文言程式」」是矣。
施「是否純中文」於「測試字符串」。名之曰「是否中文」。
書之「文言程式是否純中文：」。
書之「是否中文」。

書之「===== 算法測試完成 =====」。

書之「所有函數都基於真正的算法實現」。
書之「可以處理任意符合長度要求的輸入」。
書之「不再依賴硬編碼的模式匹配」。

書之「🎉 真正的算法實現已完成！」。