/* 測試字符串經庫 - 全面功能測試
   Author: Whisky, PR Worker
   Test all functions in the string library */

/* 導入字符串經 */
吾嘗觀「「../../libs/字符串經/字符串經.wy」」之書。方悟「「字符串經」」之義。

書之「===== 字符串經庫全面測試 =====」。

/* 測試字符串長度 */
書之「測試1：字符串長度功能」。
吾有一言。名之曰「測試字符串」。
昔之「測試字符串」者。今「「Hello文言」」是矣。

施「取字符串長度」於「測試字符串」。名之曰「長度」。
書之「字符串『Hello文言』的長度：」。
書之「長度」。

/* 測試字符串拼接 */
書之「測試2：字符串拼接功能」。
吾有一言。名之曰「甲」。
昔之「甲」者。今「「古典」」是矣。
吾有一言。名之曰「乙」。 
昔之「乙」者。今「「程序設計」」是矣。

施「拼接字符串」於「甲」於「乙」。名之曰「拼接結果」。
書之「拼接『古典』和『程序設計』：」。
書之「拼接結果」。

/* 測試字符串比較 */
書之「測試3：字符串比較功能」。
施「比較字符串」於「甲」於「乙」。名之曰「比較結果」。
書之「比較『古典』和『程序設計』：」。
書之「比較結果」。

施「比較字符串」於「甲」於「甲」。名之曰「相等結果」。
書之「比較『古典』和『古典』：」。
書之「相等結果」。

/* 測試字符串為空判斷 */
書之「測試4：字符串為空判斷」。
吾有一言。名之曰「空字符串」。
昔之「空字符串」者。今「「」」是矣。

施「字符串為空」於「空字符串」。名之曰「空判斷結果」。
書之「空字符串判斷：」。
書之「空判斷結果」。

施「字符串為空」於「甲」。名之曰「非空判斷結果」。
書之「非空字符串判斷：」。
書之「非空判斷結果」。

/* 測試取字符 */
書之「測試5：取字符功能」。
施「取字符」於「測試字符串」於一。名之曰「第一字符」。
書之「取第一個字符：」。
書之「第一字符」。

施「取字符」於「測試字符串」於三。名之曰「第三字符」。
書之「取第三個字符：」。
書之「第三字符」。

/* 測試字符串包含 */
書之「測試6：字符串包含功能」。
施「字符串包含」於「測試字符串」於「「文言」」。名之曰「包含結果」。
書之「『Hello文言』是否包含『文言』：」。
書之「包含結果」。

施「字符串包含」於「測試字符串」於「「Java」」。名之曰「不包含結果」。
書之「『Hello文言』是否包含『Java』：」。
書之「不包含結果」。

/* 測試重複字符串 */
書之「測試7：重複字符串功能」。
施「重複字符串」於「「哈」」於三。名之曰「重複結果」。
書之「重複『哈』三次：」。
書之「重複結果」。

施「重複字符串」於「「文言」」於二。名之曰「重複結果2」。
書之「重複『文言』兩次：」。
書之「重複結果2」。

/* 邊界測試 */
書之「測試8：邊界情況測試」。
施「取字符」於「測試字符串」於〇。名之曰「邊界字符1」。
書之「取第0個字符（應為空）：」。
書之「邊界字符1」。

施「重複字符串」於「「測試」」於〇。名之曰「零重複」。
書之「重複0次（應為空）：」。
書之「零重複」。

書之「===== 所有測試完成 =====」。