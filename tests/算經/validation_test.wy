/* 數學庫驗證測試 - Mathematical Library Validation Test
 * Author: Whisky, PR Worker  
 * Version: 1.1
 * Created: 2025-08-04
 * Issue: #17 - Math Library: Core Mathematical Operations
 * 
 * 本測試驗證數學運算庫的關鍵功能
 * This test validates key functionality of the math library
 */

/* 載入數學庫 */
吾嘗觀「「../../藏書樓/算經.wy」」之書。方悟是書之所云云。

書「=== 數學庫驗證測試開始 ===」。

/* 測試基本運算 */
書「測試基本運算...」。
吾有一數。名之曰「甲」。昔之「甲」者。今十也。
吾有一數。名之曰「乙」。名之曰「乙」。昔之「乙」者。今五也。

施「加」於「甲」於「乙」。名之曰「加法結果」。
書「10 + 5 = 」。書「加法結果」。

施「減」於「甲」於「乙」。名之曰「減法結果」。
書「10 - 5 = 」。書「減法結果」。

施「乘」於「甲」於「乙」。名之曰「乘法結果」。
書「10 * 5 = 」。書「乘法結果」。

施「除」於「甲」於「乙」。名之曰「除法結果」。
書「10 / 5 = 」。書「除法結果」。

/* 測試數學常數 */
書「測試數學常數...」。
書「π = 」。書「圓周率」。
書「e = 」。書「自然常數」。
書「φ = 」。書「黃金比例」。

/* 測試平方根 */
書「測試平方根...」。
吾有一數。名之曰「數值」。昔之「數值」者。今九也。
施「平方根」於「數值」。名之曰「根結果」。
書「√9 = 」。書「根結果」。

吾有一數。名之曰「數值二」。昔之「數值二」者。今十六也。
施「平方根」於「數值二」。名之曰「根結果二」。
書「√16 = 」。書「根結果二」。

/* 測試三角函數 */
書「測試三角函數...」。
吾有一數。名之曰「角度」。昔之「角度」者。今〇也。
施「正弦」於「角度」。名之曰「sin0」。
書「sin(0) = 」。書「sin0」。

施「餘弦」於「角度」。名之曰「cos0」。
書「cos(0) = 」。書「cos0」。

/* 測試度數轉換 */
書「測試度數轉換...」。
吾有一數。名之曰「九十度」。昔之「九十度」者。今九十也。
施「度轉弧度」於「九十度」。名之曰「π半」。
書「90° → 弧度 = 」。書「π半」。

施「度數正弦」於「九十度」。名之曰「sin90」。
書「sin(90°) = 」。書「sin90」。

/* 測試隨機數 */
書「測試隨機數生成...」。
施「設定隨機種子」於一二三四。名之曰「種子設定」。
施「隨機數」於一於十。名之曰「隨機值一」。
書「隨機數(1-10) = 」。書「隨機值一」。

施「隨機數」於一於十。名之曰「隨機值二」。
書「隨機數(1-10) = 」。書「隨機值二」。

/* 測試陣列統計函數 */
書「測試陣列統計...」。
吾有一列。名之曰「測試陣列」。
充「測試陣列」以一。以二。以三。以四。以五。

施「陣列平均值」於「測試陣列」。名之曰「平均值」。
書「平均值 [1,2,3,4,5] = 」。書「平均值」。

施「陣列最大值」於「測試陣列」。名之曰「最大值」。
書「最大值 [1,2,3,4,5] = 」。書「最大值」。

施「陣列最小值」於「測試陣列」。名之曰「最小值」。
書「最小值 [1,2,3,4,5] = 」。書「最小值」。

施「陣列中位數」於「測試陣列」。名之曰「中位數」。
書「中位數 [1,2,3,4,5] = 」。書「中位數」。

/* 測試向下向上取整 */
書「測試取整函數...」。
吾有一數。名之曰「小數」。昔之「小數」者。今三又五分也。
施「向下取整」於「小數」。名之曰「向下結果」。
書「floor(3.5) = 」。書「向下結果」。

施「向上取整」於「小數」。名之曰「向上結果」。
書「ceil(3.5) = 」。書「向上結果」。

施「四捨五入」於「小數」。名之曰「四捨五入結果」。
書「round(3.5) = 」。書「四捨五入結果」。

書「=== 數學庫驗證測試完成 ===」。

/* Author: Whisky, PR Worker - Comprehensive validation test for Issue #17 */