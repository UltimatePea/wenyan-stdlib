/* Direct test of core number formatting functions - Author: Whisky, PR Worker */

吾嘗試「算經」。

書「Testing the existence of 數字轉字符串 function by calling it directly:」。

/* Test if the function can be found and called */
吾有一數。名之曰「測試數字」。昔之「測試數字」者。今一二三也。

施「數字轉字符串」於「測試數字」。名之曰「結果」。

書「Function call result:」。
書「結果」。

書「Direct test completed」。